# Environment Variables for Local Testing
# Copy this to .env and fill in your values
# NOTE: .env is gitignored - never commit it!

# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# Bedrock Model
BEDROCK_MODEL_ID=meta.llama3-2-1b-instruct-v1:0

# DynamoDB Table (use local table for testing)
DYNAMODB_TABLE=payment-bot-sessions-local

# Secrets Manager (use your AWS account)
STRIPE_SECRET_ARN=arn:aws:secretsmanager:us-east-1:YOUR_ACCOUNT_ID:secret:payment-smart-bot/stripe-key-dev

# Optional: For local testing without Secrets Manager, set Stripe key directly
# STRIPE_API_KEY=sk_test_YOUR_KEY_HERE

# Lambda Configuration (simulated locally)
LAMBDA_TIMEOUT=60
LAMBDA_MEMORY=512
